---
import { Image } from "astro:assets";
import dayjs from "dayjs";

const {
  title,
  description,
  heroImage,
  badge,
  pubDate,
  url,
  tags
} = Astro.props;
const displayDate = dayjs(pubDate).format("DD/MM/YYYY")
const tag_url = '/blog/tag/'
---

<div class="rounded-lg bg-base-100 hover:shadow-xl transition ease-in-out hover:scale-[102%]">
  <a href={url}>
    <div class="hero-content flex-col md:flex-row">
      {
        heroImage && (
          <Image
            width={750}
            height={422}
            format="webp"
            src={heroImage}
            alt={title}
            class="max-w-48 md:max-w-[13rem] max-h-28 rounded-lg object-cover"
          />
        )
      }
      <div class="grow w-full md:p-0">
        <h2 class="text-xl font-bold">{title}
          {badge && <div class="badge badge-neutral rounded my-2 ml-1">{badge}</div>}  
        </h2>
        <p class="pb-1 text-1xl">{description}</p>
        <div class="text-sm opacity-70 float-left">{displayDate}</div>
        <div class="card-actions justify-end leading-[1.2]">
          {
            tags && tags.map((tag) => (
            <a href={`${tag_url}${tag}`}><span class="badge badge-outline opacity-70 hover:bg-gray-500">{tag}</span></a>
            ))
          }
        </div>

      </div>
    </div>
  </a>
</div>
